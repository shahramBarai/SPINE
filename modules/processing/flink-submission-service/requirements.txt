# Flink Submission Worker – Python dependencies
#
# Keep this minimal. This service does NOT need PyFlink itself;
# PyFlink is only needed inside the Flink cluster runtime.

fastapi>=0.111.0,<1.0.0
uvicorn[standard]>=0.29.0,<1.0.0
pydantic>=2.0.0,<3.0.0
python-multipart>=0.0.9,<1.0.0