type Triple = {
    subject: string;
    predicate: string;
    object: string;
};

type SemanticSearchExample = {
    id: string;
    label: string;
};

export type { Triple, SemanticSearchExample };
